package com.example.hms.enums;

/**
 * Workflow actions reviewers can capture on a treatment plan.
 */
public enum TreatmentPlanReviewAction {
    APPROVED,
    CHANGES_REQUESTED,
    SENT_BACK,
    SIGNED_OFF
}
