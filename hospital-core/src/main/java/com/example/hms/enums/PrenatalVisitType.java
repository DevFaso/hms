package com.example.hms.enums;

/**
 * Categorises prenatal visits so the planner can highlight intake, routine, ultrasound,
 * or late-pregnancy monitoring appointments.
 */
public enum PrenatalVisitType {
    INITIAL_INTAKE,
    ROUTINE_CHECK,
    ULTRASOUND,
    LATE_PREGNANCY
}
