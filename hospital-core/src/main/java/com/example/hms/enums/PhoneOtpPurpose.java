package com.example.hms.enums;

/** Why an SMS one-time password was issued. */
public enum PhoneOtpPurpose {
    /** Prove the patient owns the phone number typed at the registration desk. */
    REGISTRATION_PHONE_VERIFICATION
}
