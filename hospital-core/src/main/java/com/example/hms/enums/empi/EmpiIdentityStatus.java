package com.example.hms.enums.empi;

/**
 * Lifecycle status for a master identity record within the EMPI.
 */
public enum EmpiIdentityStatus {
    ACTIVE,
    MERGED,
    RETIRED,
    SUSPENDED,
    DUPLICATE
}
