package com.example.hms.enums;

/**
 * Tracks the approval state of a discharge request initiated by nursing staff.
 */
public enum DischargeStatus {
    PENDING,
    APPROVED,
    REJECTED,
    CANCELLED
}
