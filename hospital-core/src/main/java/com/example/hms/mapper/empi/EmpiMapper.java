package com.example.hms.mapper.empi;

/**
 * Legacy package placeholder for historical references.
 * The actual mapper implementation now lives in
 * {@code com.example.hms.mapper.EmpiMapper}.
 */
@SuppressWarnings("unused")
class EmpiMapperPlaceholder {
	private EmpiMapperPlaceholder() {
		throw new IllegalStateException("Use com.example.hms.mapper.EmpiMapper instead");
	}
}
