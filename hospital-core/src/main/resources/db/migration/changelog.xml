<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- =================================================================
         V1: Complete schema â€” all 10 schemas + 153 tables
         Generated from Hibernate entity model to ensure 1:1 alignment
         ================================================================= -->
    <changeSet id="V1-initial-schema" author="hms-team" runOnChange="false">
        <sqlFile path="V1__Initial_Schema.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V1.1: Unique constraints omitted by Hibernate SchemaExport
         Required before V2 (ON CONFLICT (code) needs unique index)
         ================================================================= -->
    <changeSet id="V1.1-add-unique-constraints" author="hms-team" runOnChange="false">
        <sqlFile path="V1_1__add_unique_constraints.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V2: Seed 24 role codes (reference data required at bootstrap)
         ================================================================= -->
    <changeSet id="V2-seed-roles" author="hms-team" runOnChange="false">
        <sqlFile path="V2__seed_roles.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V3: Seed DEFAULT_ORG + 6 security policies + security rules
         ================================================================= -->
    <changeSet id="V3-seed-default-org-security" author="hms-team" runOnChange="false">
        <sqlFile path="V3__seed_default_organization_security.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V4: Make hospital.staff.license_number nullable
         Non-clinical staff (RECEPTIONIST, ADMIN, etc.) do not hold a
         medical licence. Clinical-role validation is enforced at the
         application layer (StaffServiceImpl.validateLicenseNumber).
         ================================================================= -->
    <changeSet id="V4-staff-license-number-nullable" author="hms-team" runOnChange="false">
        <sqlFile path="V4__staff_license_number_nullable.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V5: Add temp_plain_password column to user_role_hospital_assignment
         Stores the auto-generated plaintext credential only until the user
         verifies their assignment code; cleared immediately after use.
         ================================================================= -->
    <changeSet id="V5-add-temp-plain-password" author="hms-team" runOnChange="false">
        <sqlFile path="V5__add_temp_plain_password.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V6: Repair soft-deleted users who still have active role assignments
         Restores is_deleted=false / is_active=true for any user who is
         soft-deleted but owns at least one active UserRoleHospitalAssignment.
         ================================================================= -->
    <changeSet id="V6-repair-soft-deleted-active-users" author="hms-team" runOnChange="false">
        <!-- validCheckSum: accept the old hash (a.active = true) that was already
             recorded in DATABASECHANGELOG on the dev/uat DB before the column-name
             typo was corrected to a.is_active.  The new hash is accepted automatically.
             Old:  9:675be9a24a8d91736e78ec3f93922422
             New:  9:fd056d3ca3f30133f1ca8fff983343fa  -->
        <validCheckSum>9:675be9a24a8d91736e78ec3f93922422</validCheckSum>
        <sqlFile path="V6__repair_soft_deleted_active_users.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V7: Make chat_messages.assignment_id nullable
         SUPER_ADMIN and users without hospital context may send direct
         messages. assignment is left null in that path; the initial
         schema had NOT NULL which caused a 400 constraint violation.
         ================================================================= -->
    <changeSet id="V7-chat-assignment-nullable" author="hms-team" runOnChange="false">
        <sqlFile path="V7__chat_assignment_nullable.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V8: Fix duplicate staff rows + normalize legacy enum values
         - Deduplicates hospital.staff by keeping the latest row per user_id
         - Re-points departments.head_of_department_staff_id to canonical row
         - Adds UNIQUE INDEX ON hospital.staff(user_id)
         - Normalizes REGISTERED â†’ ADMITTED in patient_hospital_registrations
         - Normalizes LAB_SCIENTIST â†’ LABORATORY_SCIENTIST in staff
         ================================================================= -->
    <changeSet id="V8-fix-staff-unique-normalize-stay-status" author="hms-team" runOnChange="false">
        <sqlFile path="V8__fix_staff_unique_and_normalize_stay_status.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V9: Support SYSTEM actor in audit_event_logs
         - Makes user_id nullable so bootstrap/scheduled events can be
           persisted without a real user entity.
         - Adds actor_type (USER|SYSTEM) and actor_label columns.
         - CHECK constraint ensures USER events still require user_id.
         ================================================================= -->
    <changeSet id="V9-audit-system-actor-support" author="hms-team" runOnChange="false">
        <sqlFile path="V9__audit_system_actor_support.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V10: Activate hospital-scoped staff assignments created by admin
         Admin-register created assignments with is_active=false due to
         enforceRoleScopeConstraints() confirmation-code workflow. This
         broke JWT hospital claims and /api/me/hospital for all staff.
         ================================================================= -->
    <changeSet id="V10-activate-admin-staff-assignments" author="hms-team" runOnChange="true">
        <sqlFile path="V10__activate_admin_registered_staff_assignments.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V11: Add receiving_hospital_id and source_department_id to
         general_referrals for the redesigned referral workflow with
         explicit source/destination hospital and department tracking.
         ================================================================= -->
    <changeSet id="V11-add-referral-receiving-hospital-source-department" author="hms-team" runOnChange="false">
        <sqlFile path="V11__add_referral_receiving_hospital_source_department.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>


    <!-- =================================================================
         V12: Add force_username_change flag to security.users
         Allows admins to require a username change on next login.
         ================================================================= -->
    <changeSet id="V12-add-force-username-change" author="hms-team" runOnChange="false">
        <sqlFile path="V12__add_force_username_change.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V13: Add clinical-triage columns for Physician Cockpit
         - lab.lab_results.abnormal_flag (NORMAL / ABNORMAL / CRITICAL)
         - clinical.patients.code_status (FULL_CODE / DNR / DNI / COMFORT_ONLY)
         - clinical.encounters.urgency (EMERGENT / URGENT / ROUTINE / LOW)
         ================================================================= -->
    <changeSet id="V13-add-abnormal-flag-code-status-encounter-urgency" author="hms-team" runOnChange="false">
        <sqlFile path="V13__add_abnormal_flag_code_status_encounter_urgency.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V14: Add Physician Cockpit tables
         - clinical.patient_diagnoses (active/historical diagnoses + ICD codes)
         - clinical.clinical_alerts (patient safety + triage alerts)
         - hospital.on_call_schedule (staff on-call roster with time range)
         ================================================================= -->
    <changeSet id="V14-add-patient-diagnoses-alerts-oncall" author="hms-team" runOnChange="false">
        <sqlFile path="V14__add_patient_diagnoses_alerts_oncall.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V15: Add clinical.medication_administration_records
         Tracks per-dose nursing administration events linked to a
         Prescription. Status: PENDING | GIVEN | HELD | REFUSED | MISSED.
         ================================================================= -->
    <changeSet id="V15-add-medication-administration-records" author="hms-team" runOnChange="false">
        <sqlFile path="V15__add_medication_administration_records.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V16: Add missing updated_at to hospital.on_call_schedule
         BaseEntity requires updated_at; V14 only created created_at.
         ================================================================= -->
    <changeSet id="V16-add-updated-at-on-call-schedule" author="hms-team" runOnChange="false">
        <sqlFile path="V16__add_updated_at_on_call_schedule.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V17: Add assignment tracking fields to clinical.consultations
         Tracks when a consultation was assigned and who performed it.
         Required by ConsultationServiceImpl.assignConsultation().
         ================================================================= -->
    <changeSet id="V17-add-consultation-assignment-tracking" author="hms-team" runOnChange="false">
        <sqlFile path="V17__add_consultation_assignment_tracking.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V18: Add workflow progress fields to clinical.consultations
         - started_at: set when consultant begins the consult
         - declined_at / decline_reason: set when consultant declines
         ================================================================= -->
    <changeSet id="V18-add-consultation-workflow-fields" author="hms-team" runOnChange="false">
        <sqlFile path="V18__add_consultation_workflow_fields.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V19: MVP 11 â€” Waitlist management + insurance attestation
         Adds reception.waitlist table and insurance verification columns.
         ================================================================= -->
    <changeSet id="V19-mvp11-waitlist-and-insurance-attestation" author="hms-team" runOnChange="false">
        <sqlFile path="V19__mvp11_waitlist_and_insurance_attestation.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V20: MVP 12 â€” Nurse Station Phase 2 indexes
         Composite indexes for admissions + patient vital signs lookups.
         ================================================================= -->
    <changeSet id="V20-mvp12-nurse-station-phase2" author="hms-team" runOnChange="false">
        <sqlFile path="V20__mvp12_nurse_station_phase2.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V21: MVP 13 â€” Nursing Task Board
         Creates clinical.nursing_tasks table for bedside care tracking.
         ================================================================= -->
    <changeSet id="V21-mvp13-nursing-tasks" author="hms-team" runOnChange="false">
        <sqlFile path="V21__mvp13_nursing_tasks.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V22: MVP 14 â€” Proxy/Family Access + Notification Preferences
         - clinical.patient_proxies: proxy/family linked-account grants
         - security.notification_preferences: per-user delivery settings
         - security.notifications.type column for categorisation
         ================================================================= -->
    <changeSet id="V22-mvp14-proxy-access-notification-preferences" author="hms-team" runOnChange="false">
        <sqlFile path="V22__mvp14_proxy_access_notification_preferences.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V23: MVP 19 â€” Staff Compliance: license_expiry_date on staff
         ================================================================= -->
    <changeSet id="V23-mvp19-staff-license-expiry" author="hms-team" runOnChange="false">
        <sqlFile path="V23__mvp19_staff_license_expiry.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V24: MVP 20 â€” Payment Transaction Ledger
         ================================================================= -->
    <changeSet id="V24-mvp20-payment-transactions" author="hms-team" runOnChange="false">
        <sqlFile path="V24__mvp20_payment_transactions.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V25: Ward & Bed Management
         ================================================================= -->
    <changeSet id="V25-ward-bed-management" author="hms-team" runOnChange="false">
        <sqlFile path="V25__ward_bed_management.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V26: MVP1 â€” Add Lab Technician and Lab Manager Roles
         Seeds ROLE_LAB_TECHNICIAN and ROLE_LAB_MANAGER into security.roles.
         ================================================================= -->
    <changeSet id="V26-add-lab-technician-manager-roles" author="hms-team" runOnChange="false">
        <sqlFile path="V26__add_lab_technician_manager_roles.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V27: MVP2 â€” Lab Order Priority + Specimen Tracking
         - Adds priority VARCHAR(20) NOT NULL DEFAULT 'ROUTINE' to lab.lab_orders
         - Creates lab.lab_specimens table with accession number and custody fields
         ================================================================= -->
    <changeSet id="V27-mvp2-lab-order-priority-and-specimens" author="hms-team" runOnChange="false">
        <sqlFile path="V27__mvp2_lab_order_priority_and_specimens.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V28: MVP3 â€” Instrument Integration, QC Events & Reflex Rules
         - Creates lab.instrument_outbox (outbound HL7v2 queue)
         - Creates lab.qc_events (analyzer calibration tracking)
         - Creates lab.reflex_rules (auto add-on test rules)
         ================================================================= -->
    <changeSet id="V28-mvp3-instrument-outbox-qc-reflex" author="hms-team" runOnChange="false">
        <sqlFile path="V28__mvp3_instrument_outbox_qc_reflex.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V29: Hotfix â€” add missing updated_at to lab.instrument_outbox
         BaseEntity requires updated_at on every table; V28 omitted it.
         ================================================================= -->
    <changeSet id="V29-fix-instrument-outbox-updated-at" author="hms-team" runOnChange="false">
        <sqlFile path="V29__fix_instrument_outbox_updated_at.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V30: Lab Director Approval Workflow
         - Seeds ROLE_LAB_DIRECTOR and ROLE_QUALITY_MANAGER
         - Adds approval_status, approved_by_id, approved_at,
           reviewed_by_id, reviewed_at, rejection_reason to
           lab.lab_test_definitions
         - Status lifecycle: DRAFT â†’ PENDING_QA_REVIEW â†’
           PENDING_DIRECTOR_APPROVAL â†’ APPROVED â†’ ACTIVE â†’ RETIRED
         - Grandfathers existing active definitions as APPROVED
         ================================================================= -->
    <changeSet id="V30-lab-director-approval-workflow" author="hms-team" runOnChange="false">
        <sqlFile path="V30__lab_director_approval_workflow.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V31: Lab Test Validation Studies (CLIA/CLSI)
         - Creates lab.lab_test_validation_studies table
         - Records formal validation study events per test definition
           (precision, accuracy, reference range, method comparison, etc.)
         - FK to lab.lab_test_definitions
         ================================================================= -->
    <changeSet id="V31-lab-test-validation-studies" author="hms-team" runOnChange="false">
        <sqlFile path="V31__lab_test_validation_studies.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V32: Patient Uploaded Documents
         - Creates clinical.patient_uploaded_documents table
         - Allows patients/proxies to upload medical documents via portal
         - Stores metadata only; files live on server filesystem
         ================================================================= -->
    <changeSet id="V32-patient-uploaded-documents" author="hms-team" runOnChange="false">
        <sqlFile path="V32__patient_uploaded_documents.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V33: Consent type/scope columns + audit performance indexes
         - Adds consent_type column to clinical.patient_consents
         - Adds scope column to clinical.patient_consents
         - Adds performance indexes on audit.audit_event_logs
         ================================================================= -->
    <changeSet id="V33-consent-type-scope-audit-indexes" author="hms-team" runOnChange="false">
        <sqlFile path="V33__consent_type_scope_and_audit_indexes.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V34: Fix Patient Uploaded Documents - Correct schema reference
         - Fixes V32 which referenced public.users instead of security.users
         - Drops and recreates clinical.patient_uploaded_documents with correct FK
         ================================================================= -->
    <changeSet id="V34-fix-patient-uploaded-documents-schema" author="hms-team" runOnChange="false">
        <sqlFile path="V34__fix_patient_uploaded_documents_schema.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V35: Lab Instrument & Inventory Management (MVP 4)
         - Creates lab.lab_instruments table for tracking instruments,
           calibration schedules, and maintenance windows
         - Creates lab.lab_inventory_items table for reagents,
           consumables, and reorder-threshold alerts
         - Adds unique constraints, FK references, and performance indexes
         ================================================================= -->
    <changeSet id="V35-lab-instrument-inventory" author="hms-team" runOnChange="false">
        <sqlFile path="V35__lab_instrument_inventory.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V36: MVP 1 â€” Patient Check-In & Arrival Workflow
         - Adds checked_in_at TIMESTAMP to clinical.appointments
         - Adds arrival_timestamp TIMESTAMP to clinical.encounters
         - Adds chief_complaint VARCHAR(2048) to clinical.encounters
         ================================================================= -->
    <changeSet id="V36-mvp1-checkin-columns" author="hms-team" runOnChange="false">
        <sqlFile path="V36__mvp1_checkin_columns.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V37: MVP 2 â€” Triage & Rooming Workflow
         - Adds esi_score INTEGER to clinical.encounters (CHECK 1-5)
         - Adds room_assignment VARCHAR(100) to clinical.encounters
         - Adds triage_timestamp TIMESTAMP to clinical.encounters
         - Adds roomed_timestamp TIMESTAMP to clinical.encounters
         - Adds partial indexes on room_assignment and triage_timestamp
         ================================================================= -->
    <changeSet id="V37-mvp2-triage-columns" author="hms-team" runOnChange="false">
        <sqlFile path="V37__mvp2_triage_columns.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V38: MVP 3 â€” Nursing Intake Flowsheet
         - Adds nursing_intake_timestamp TIMESTAMP to clinical.encounters
         - Partial index on nursing_intake_timestamp
         ================================================================= -->
    <changeSet id="V38-mvp3-nursing-intake-column" author="hms-team" runOnChange="false">
        <sqlFile path="V38__mvp3_nursing_intake_column.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V39: MVP 4 â€” Pre-Visit Questionnaires & Pre-Check-In
         - New questionnaires table (admin-managed templates)
         - New questionnaire_responses table (patient-submitted)
         - Adds pre_checked_in, pre_checkin_timestamp to appointments
         ================================================================= -->
    <changeSet id="V39-mvp4-questionnaires-precheckin" author="hms-team" runOnChange="false">
        <sqlFile path="V39__mvp4_questionnaires_and_precheckin.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V40: MVP 6 â€” Check-Out & After-Visit Summary (AVS)
         - Adds checkout_timestamp to encounters
         - Adds follow_up_instructions (free-text)
         - Adds discharge_diagnoses (JSON text)
         ================================================================= -->
    <changeSet id="V40-mvp6-checkout-avs" author="hms-team" runOnChange="false">
        <sqlFile path="V40__mvp6_checkout_avs.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V41: MVP 7 â€” Provider In-Basket & Result Notification
         - Creates clinical.in_basket_items table
         - Indexes for recipient, status, priority, reference lookups
         ================================================================= -->
    <changeSet id="V41-mvp7-in-basket-items" author="hms-team" runOnChange="false">
        <sqlFile path="V41__mvp7_in_basket_items.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V42: Repair â€” ensure clinical.questionnaire_responses has the
         "responses" column.  The table may have been pre-created by
         V1/Hibernate before V39 ran, so CREATE TABLE IF NOT EXISTS
         silently skipped the column.
         ================================================================= -->
    <changeSet id="V42-repair-questionnaire-responses-column" author="hms-team" runOnChange="false">
        <sqlFile path="V42__repair_questionnaire_responses_column.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V43a: Password history for security hardening
         ================================================================= -->
    <changeSet id="V43-password-history" author="hms-team" runOnChange="false">
        <sqlFile path="V43__password_history.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V43b: Pharmacy Module â€” Phase 1 Foundation
         ================================================================= -->
    <changeSet id="V43-pharmacy-module-phase1" author="hms-team" runOnChange="false">
        <sqlFile path="V43__pharmacy_module_phase1.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V44: MFA TOTP secret and backup codes
         ================================================================= -->
    <changeSet id="V44-mfa-totp-backup-codes" author="hms-team" runOnChange="false">
        <sqlFile path="V44__mfa_totp_backup_codes.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V45: Pharmacy medication catalog
         ================================================================= -->
    <changeSet id="V45-pharmacy-medication-catalog" author="hms-team" runOnChange="false">
        <sqlFile path="V45__pharmacy_medication_catalog.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V46: Pharmacy registry
         ================================================================= -->
    <changeSet id="V46-pharmacy-registry" author="hms-team" runOnChange="false">
        <sqlFile path="V46__pharmacy_registry.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V47â€“V49: reservation cancelled. The three phases they were held
         for â€” dispense, inventory and claims â€” all shipped in V43
         (clinical.dispenses, clinical.inventory_items,
         billing.pharmacy_claims, billing.pharmacy_payments), so the
         reservation was stale and the placeholder files were empty. They
         have been deleted rather than left as three zero-byte migrations
         that look registered-but-inert: writing SQL into one would have
         run nothing, silently, because no changeSet referenced it.
         ================================================================= -->

    <!-- =================================================================
         V50: Schema fixes (Copilot review)
         - mfa_backup_codes: add missing updated_at column
         - Drop orphaned hospital.pharmacies table
         ================================================================= -->
    <changeSet id="V50-schema-fixes" author="hms-team" runOnChange="false">
        <sqlFile path="V50__schema_fixes.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V51: Recovery contact verification fields
         - Add verification_code_hash, verification_code_expires_at,
           verification_attempts to security.user_recovery_contacts
         - Reset auto-verified contacts pending proper verification
         ================================================================= -->
    <changeSet id="V51-recovery-contact-verification" author="hms-team" runOnChange="false">
        <sqlFile path="V51__recovery_contact_verification.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V52: Prescription routing decisions (Phase 4b stock-out routing)
         - Create clinical.prescription_routing_decisions table
         - Indexes: prescription_id, patient_id, status
         ================================================================= -->
    <changeSet id="V52-prescription-routing-decisions" author="hms-team" runOnChange="false">
        <sqlFile path="V52__prescription_routing_decisions.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V53: S-05 Phase 2 (slice 1) â€” encrypt clinical.dispenses.notes
         - Widen notes from VARCHAR(1000) to TEXT to fit AES-256-GCM
           Base64 ciphertext (entity still enforces @Size(max=1000) on
           plaintext input).
         ================================================================= -->
    <changeSet id="V53-encrypt-dispense-notes" author="hms-team" runOnChange="false">
        <sqlFile path="V53__encrypt_dispense_notes.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V54: S-05 Phase 2 (slice 2) â€” encrypt Prescription PHI fields
         - Widen notes / override_reason / instructions to TEXT to fit
           AES-256-GCM Base64 ciphertext (entity still enforces @Size on
           plaintext input).
         ================================================================= -->
    <changeSet id="V54-encrypt-prescription-phi" author="hms-team" runOnChange="false">
        <sqlFile path="V54__encrypt_prescription_phi.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V55: S-05 Phase 2 (slice 3) â€” encrypt Patient PHI free-text fields
         - Widen address, address_line1/2, emergency_contact_*, allergies,
           medical_history_summary, care_team_notes, chronic_conditions to
           TEXT to fit AES-256-GCM Base64 ciphertext.
         - Phone numbers and email are deliberately NOT encrypted because
           they are used in LIKE / equality lookups, unique constraints, and
           an index.
         ================================================================= -->
    <changeSet id="V55-encrypt-patient-phi" author="hms-team" runOnChange="false">
        <sqlFile path="V55__encrypt_patient_phi.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V56: Keycloak identity link columns on security.users
         - keycloak_subject VARCHAR(128) NULL â€” the KC JWT 'sub' claim.
           Partial unique index (WHERE keycloak_subject IS NOT NULL) so
           legacy/unlinked users do not conflict.
         - keycloak_realm VARCHAR(100) NULL â€” KC realm name (e.g. 'hms').
         - auth_source VARCHAR(32) NOT NULL DEFAULT 'internal' â€” drives
           which auth path the resource server trusts: 'internal' |
           'keycloak' | 'saml'.
         - last_oidc_login_at TIMESTAMPTZ NULL â€” updated by
           /auth/session/bootstrap on each OIDC login.
         ================================================================= -->
    <changeSet id="V56-keycloak-identity-link" author="hms-team" runOnChange="false">
        <sqlFile path="V56__keycloak_identity_link.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V57: P-07 â€” Pharmacy OTC walk-in sale (header + line items)
         - clinical.pharmacy_sales: cash transactions not tied to a
           prescription (anonymous walk-in patient_id is allowed).
         - clinical.sale_lines: per-medication line items with unit price
           captured at sale time so catalog price changes do not rewrite
           historical revenue.
         ================================================================= -->
    <changeSet id="V57-pharmacy-sale-sale-line" author="hms-team" runOnChange="false">
        <sqlFile path="V57__pharmacy_sale_sale_line.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V58: P-09 â€” Medication Therapy Management (MTM) review record
         - clinical.mtm_reviews: pharmacist-led chronic disease review
           with adherence concern flag, polypharmacy alert, intervention
           summary, recommended actions, and status (DRAFT/COMPLETED/REFERRED).
         ================================================================= -->
    <changeSet id="V58-mtm-review" author="hms-team" runOnChange="false">
        <sqlFile path="V58__mtm_review.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V59: FU-2 (P-06 backend) â€” first-class stock RECEIPT columns
         - Adds lot_number, supplier, po_reference, expiry_date, unit_cost
           to clinical.stock_transactions so reporting can query these
           fields directly instead of parsing the free-text `reason`.
         - Additive only; all columns nullable. Existing rows unchanged.
         ================================================================= -->
    <changeSet id="V59-stock-transaction-receipt-fields" author="hms-team" runOnChange="false">
        <sqlFile path="V59__stock_transaction_receipt_fields.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V60: P1 #1 â€” Terminology binding (LOINC on lab test definitions)
         - Adds nullable loinc_code (VARCHAR 20) and loinc_display
           (VARCHAR 255) to lab.lab_test_definitions plus a partial
           index on loinc_code (PostgreSQL).
         - LOINC format is enforced at the application layer via
           TerminologyCodes#isValidLoinc.
         - Additive only; ATC + RxNorm + icd_version already exist on
           clinical.medication_catalog_items / clinical.patient_problems.
         ================================================================= -->
    <changeSet id="V60-terminology-binding" author="hms-team" runOnChange="false">
        <sqlFile path="V60__terminology_binding.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V61: P1 #2a â€” SYSTEM actor support on lab.lab_results
         - Mirrors V9's pattern for audit_event_logs.
         - Makes assignment_id nullable so machine-origin LabResult
           writes (HL7 v2 MLLP / external LIS in P1 #2b) can persist
           without a hospital-scoped human assignment.
         - Adds actor_type (USER|SYSTEM) and actor_label columns.
         - CHECK constraint ensures USER rows still require an
           assignment_id, preserving the existing clinical guarantee.
         ================================================================= -->
    <changeSet id="V61-system-actor-on-lab-results" author="hms-team" runOnChange="false">
        <sqlFile path="V61__system_actor_on_lab_results.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V62: P1 #2b â€” MLLP per-facility allowlist
         - platform.mllp_allowed_senders maps (sending_application,
           sending_facility) MSH-3/MSH-4 pairs to a receiving hospital.
         - The dispatcher uses this to reject unknown senders with AR
           and to pin accepted messages to the right hospital scope.
         - Depends on V61 (SYSTEM-actor support on lab_results) so
           machine-origin LabResult writes can omit assignment_id.
         - Additive only.
         ================================================================= -->
    <changeSet id="V62-mllp-allowed-senders" author="hms-team" runOnChange="false">
        <sqlFile path="V62__mllp_allowed_senders.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V63: P1 #3 â€” CDS rule-engine reference data
         - clinical.drug_interactions: symmetric (rxnorm_a, rxnorm_b)
           pairs with severity + advice. Seeded with 12 high-impact
           interactions from WHO Model Formulary / BNF / FDA labels.
         - clinical.medication_catalog_items.pediatric_max_dose_mg_per_kg:
           optional weight-based ceiling consulted by PediatricDoseRule.
         - Additive only; idempotent (CREATE IF NOT EXISTS / ON CONFLICT).
         ================================================================= -->
    <changeSet id="V63-cds-drug-interactions" author="hms-team" runOnChange="false">
        <!--
            splitStatements MUST be false: V63 contains a `DO $$ ... $$;`
            anonymous PL/pgSQL block to drive the bidirectional dedupe on
            the seed insert. With splitStatements=true Liquibase chops on
            the inner `;` characters and tries to execute fragments such
            as "DO $$ DECLARE v_now TIMESTAMP WITHOUT TIME ZONE := NOW()"
            on their own â€” failing with `Unterminated dollar quote
            started at position 3 in SQL DO $$`. Same pattern as V9 and
            V61, both of which use splitStatements=false for the same
            reason.
        -->
        <sqlFile path="V63__cds_drug_interactions.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V64: P1 #3b â€” BPA / protocol-card reference data
         - clinical.bpa_protocols: catalog of best-practice advisories
           rendered by BpaRule implementations on the patient-view hook.
         - Three seeded protocols: MALARIA_FEVER, SEPSIS_QSOFA,
           OB_HEMORRHAGE. Active-only partial index for rule lookups.
         - Additive only; idempotent (CREATE IF NOT EXISTS / ON CONFLICT).
         splitStatements="true" is safe here: V64 has no PL/pgSQL
         DO blocks, only plain DDL + INSERT â€¦ ON CONFLICT statements.
         ================================================================= -->
    <changeSet id="V64-bpa-protocol-reference" author="hms-team" runOnChange="false">
        <sqlFile path="V64__bpa_protocol_reference.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V65: P1 #4 â€” CPOE order-set version history
         - admission_order_sets.parent_order_set_id self-FK so each edit
           can freeze the previous row and append a new active row that
           points at it (chain forms the version history).
         - Active-by-(hospital,name) partial index speeds the picker's
           lookup.
         - Additive only; pure DDL.
         ================================================================= -->
    <changeSet id="V65-order-set-versioning" author="hms-team" runOnChange="false">
        <sqlFile path="V65__order_set_versioning.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V66: P1 #8 â€” Inpatient eMAR five-rights barcode-scan loop
         - Additive columns on clinical.medication_administration_records:
           patient_scan_value, medication_scan_value, dose_scan_value,
           scan_verified_at, five_rights_status, five_rights_overrides
           (JSONB), override_reason.
         - Indexes on five_rights_status and scan_verified_at to support
           the bedside dashboard and audit-by-override-decision queries.
         - Additive only; no destructive ops.
         ================================================================= -->
    <changeSet id="V66-inpatient-emar-five-rights" author="hms-team" runOnChange="false">
        <sqlFile path="V66__inpatient_emar_five_rights.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V67: P1 #9 â€” Break-the-glass emergency-access sessions
         - clinical.break_glass_sessions: short-lived (default 4h)
           emergency overrides granted to clinicians who need to read a
           patient's chart without a pre-existing consent.
         - Every read served under an active session emits a
           BREAK_GLASS_ACCESS audit event (existing AuditEventType).
         - Partial indexes target the (user, patient) hot path and the
           per-patient banner lookup.
         - Additive only; pure DDL.
         ================================================================= -->
    <changeSet id="V67-break-glass-sessions" author="hms-team" runOnChange="false">
        <sqlFile path="V67__break_glass_sessions.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V68: P1 #11 â€” DHIS2 ADX (Aggregate Data Exchange) export
         - integration schema (new) holds four tables: per-facility
           DHIS2 endpoint config, HMS->DHIS2 dataElement mappings,
           one row per triggered export run, and the per-value outbox
           that makes replays idempotent.
         - hospital.hospitals.dhis2_org_unit_uid binds an HMS facility
           to its DHIS2 organisation-unit UID.
         - Auth secrets are NOT stored: auth_secret_env_var holds the
           env-var name; the value is resolved at push time.
         - Additive only; pure DDL.
         ================================================================= -->
    <changeSet id="V68-dhis2-adx-export" author="hms-team" runOnChange="false">
        <sqlFile path="V68__dhis2_adx_export.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V69: P1 #10 â€” Telehealth low-bandwidth (audio + photo on chat)
         - support.chat_attachments: per-message attachment rows for
           PHOTO (â‰¤10 MB JPEG/PNG/WebP) or AUDIO (â‰¤5 MB Opus/AAC/MP3,
           â‰¤90 s) so chat threads can carry remote-consult media without
           introducing WebRTC.
         - ON DELETE CASCADE keeps orphan rows out when a message is
           soft/hard-deleted.
         - Partial CHECK enforces that AUDIO carries duration_seconds
           and PHOTO does not.
         - Additive only; pure DDL.
         ================================================================= -->
    <changeSet id="V69-telehealth-chat-attachments" author="hms-team" runOnChange="false">
        <sqlFile path="V69__telehealth_chat_attachments.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V70: P1 #12 â€” Referral lifecycle (Tier A close-out)
         - general_referrals.started_at TIMESTAMP NULL captures the
           IN_PROGRESS transition timestamp (set when the receiving
           clinician clicks Start). Pairs with the entity's state-machine
           guards so SCHEDULED â†’ IN_PROGRESS â†’ COMPLETED is observable.
         - Additive only; pure DDL; nullable column.
         ================================================================= -->
    <changeSet id="V70-add-referral-started-at" author="hms-team" runOnChange="false">
        <sqlFile path="V70__add_referral_started_at.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V71: P1 #12 follow-up â€” ReferralEvent audit trail
         - public.referral_events: one append-only row per state-machine
           transition on general_referrals (submit / acknowledge /
           schedule / start / complete / reject / cancel / expire).
         - referral_id is a raw UUID (no FK) so audit rows outlive
           hard-deletes of the parent referral, matching the
           encounter_note_history pattern.
         - actor_label follows the LabResult system-actor convention:
           USER for an authenticated principal, SYSTEM:<source> for
           non-user writers (e.g. the @Scheduled SLA sweep).
         - Composite index on (referral_id, recorded_at) so the
           per-referral timeline read is a single index scan.
         - Additive only; pure DDL.
         ================================================================= -->
    <changeSet id="V71-referral-events" author="hms-team" runOnChange="false">
        <sqlFile path="V71__referral_events.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V72: P1 #12 follow-up â€” JPA optimistic-lock version on general_referrals
         - public.general_referrals.version BIGINT NOT NULL DEFAULT 0
         - Required by the EXPIRED auto-sweep: without @Version, a
           concurrent transaction that flips status between the sweep's
           SELECT and its UPDATE would be silently overwritten (lost
           update). With @Version, the second writer's flush throws
           ObjectOptimisticLockingFailureException and the sweep skips
           the row.
         - Additive only; pure DDL.
         ================================================================= -->
    <changeSet id="V72-general-referrals-version" author="hms-team" runOnChange="false">
        <sqlFile path="V72__general_referrals_version.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V73: P1 #12 follow-up #4 â€” Real-time eligibility / prior-auth API
         - clinical.eligibility_checks: one row per call to a public-payer
           scheme (NHIS, NHIA, CNAMGS, mutuelle) or generic private payer.
         - Most-recent row per (patient_id, scheme, check_type) is the
           authoritative current state.
         - @Version (BIGINT NOT NULL DEFAULT 0) protects against the rare
           race where two clinicians submit a check simultaneously.
         - Composite (patient_id, scheme, completed_at) drives the "fresh
           answer?" lookup on the encounter and checkout screens.
         - Additive only; pure DDL.
         ================================================================= -->
    <changeSet id="V73-eligibility-checks" author="hms-team" runOnChange="false">
        <sqlFile path="V73__eligibility_checks.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V74: SmartPhrase / dot-phrase macro library (item 6)
         - clinical.smart_phrases stores the macro library used by the
           per-section EncounterNote form (item 5). Triggers like ".normexam"
           expand to a multi-line block when typed in any note section.
         - Scope precedence USER > HOSPITAL > GLOBAL is enforced in the
           service; the table just stores the rows.
         - Composite (scope, hospital_id, owner_user_id, lower(trigger))
           drives the autocomplete lookup.
         - Additive only; pure DDL.
         ================================================================= -->
    <changeSet id="V74-smart-phrases" author="hms-team" runOnChange="false">
        <sqlFile path="V74__smart_phrases.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V75: Gap #23 â€” DICOM/PACS link
         - hospital.hospitals.pacs_viewer_url_template VARCHAR(500) NULL
           holds a per-hospital URL template (e.g.
           https://orthanc.local/viewer.html?studyUid={studyInstanceUid}).
           The ImagingReport mapper resolves {studyInstanceUid} and
           {accessionNumber} placeholders so the imaging detail panel
           surfaces a "View in PACS" link without per-report URLs.
         - Optional: hospitals without a template render no link, which
           preserves the deferred-for-district-level posture.
         - Additive only; pure DDL.
         ================================================================= -->
    <changeSet id="V75-pacs-viewer-url-template" author="hms-team" runOnChange="false">
        <sqlFile path="V75__pacs_viewer_url_template.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V76: Gap #18 â€” BPA / CDS card acknowledgements
         - clinical.cds_acknowledgements: one row per dismissal or override
           of a Best-Practice Advisory. Drives suppression on subsequent
           rule-engine runs (24 h ack TTL, 72 h override TTL â€” set in the
           service layer).
         - Cards are matched by stable cardUuid when present; otherwise
           by the (summary, indicator) fingerprint scoped to the patient.
         - updated_at NOT NULL is required because the entity extends
           BaseEntity which maps both createdAt and updatedAt.
         - Additive only; pure DDL.
         ================================================================= -->
    <changeSet id="V76-cds-acknowledgements" author="hms-team" runOnChange="false">
        <sqlFile path="V76__cds_acknowledgements.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V77 â€” Tenant lifecycle (MVP-2)
         Adds suspend / archive / restore / schedule-purge state machine
         columns to organizations. Strictly additive; default 'ACTIVE'.
         ================================================================= -->
    <changeSet id="V77-tenant-lifecycle" author="hms-team" runOnChange="false">
        <sqlFile path="V77__tenant_lifecycle.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V78 â€” Integration health snapshots (MVP-3)
         Adds clinical.integration_health_snapshots so the super-admin
         Integration Health Console can show last-success / last-failure /
         24h counts per (integration, organization). Strictly additive.
         ================================================================= -->
    <changeSet id="V78-integration-health-snapshots" author="hms-team" runOnChange="false">
        <sqlFile path="V78__integration_health_snapshots.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V79 â€” Impersonator stamp on audit events (MVP-4)
         Adds impersonator_user_id + impersonator_username columns to
         support.audit_event_logs so every action a super admin takes
         while impersonating is traceable back to the real human.
         Strictly additive; pre-existing rows stay at NULL.
         ================================================================= -->
    <changeSet id="V79-audit-impersonator" author="hms-team" runOnChange="false">
        <sqlFile path="V79__audit_impersonator.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V80 â€” Emergency global controls (MVP-7)
         Singleton security.security_revocations table. Holds the
         `global_min_token_iat` timestamp that JwtAuthenticationFilter
         compares the JWT `iat` claim against. Bumping this value to
         "now" force-logs-out every active session.
         Strictly additive; default `epoch` so pre-MVP-7 deployments
         behave identically.
         ================================================================= -->
    <changeSet id="V80-emergency-global-controls" author="hms-team" runOnChange="false">
        <sqlFile path="V80__emergency_global_controls.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V81 â€” Subscription plans + organization subscriptions (MVP-6)
         Two new tables in the platform schema: subscription_plans
         (catalogue) and organization_subscriptions (per-org assignment).
         Partial unique index uq_orgsub_active_per_org keeps one ACTIVE
         subscription per organization while letting CANCELLED / EXPIRED
         rows accumulate for billing audits. FK back to
         hospital.organizations(id).
         Strictly additive; pure DDL.
         ================================================================= -->
    <changeSet id="V81-subscription-plans" author="hms-team" runOnChange="false">
        <sqlFile path="V81__subscription_plans.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V82 â€” Data-residency / region tagging on Organization (MVP-9)
         Adds hospital.organizations.region VARCHAR(32) NOT NULL DEFAULT
         'BF' so the super-admin Control Tower can surface the per-tenant
         jurisdiction whose data-protection rules apply (BF / CI / SN /
         GA / CM / EU / etc.). Indexed for the org-grid region filter.
         Strictly additive; default backfills legacy rows in-line.
         ================================================================= -->
    <changeSet id="V82-organization-region" author="hms-team" runOnChange="false">
        <sqlFile path="V82__organization_region.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V83 â€” Per-tenant feature-flag overrides (MVP-7b)
         Adds organization_id NULL to FeatureFlagOverride so a flag key
         can carry both a global row (org NULL) and per-tenant rows.
         Replaces the single-column UNIQUE(flag_key) with a composite
         UNIQUE(flag_key, organization_id) so global + per-tenant rows
         coexist. Strictly additive; rollback path documented in the
         SQL file.
         ================================================================= -->
    <changeSet id="V83-feature-flag-override-organization" author="hms-team" runOnChange="false">
        <sqlFile path="V83__feature_flag_override_organization.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V84 â€” Hospital-level lifecycle (MVP-c batch)
         Mirrors V77's Organization lifecycle columns onto Hospital so a
         super admin can suspend / archive / restore / schedule-purge an
         individual hospital independently of its parent organization.
         Strictly additive.
         ================================================================= -->
    <changeSet id="V84-hospital-lifecycle" author="hms-team" runOnChange="false">
        <sqlFile path="V84__hospital_lifecycle.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V85 â€” jsonb feature_keys on subscription_plans (MVP-c batch / MVP-6c)
         Adds feature_keys_jsonb JSONB and backfills from the legacy
         comma-separated TEXT column. Legacy column kept one release for
         rollback safety. Strictly additive.
         ================================================================= -->
    <changeSet id="V85-subscription-plan-feature-keys-jsonb" author="hms-team" runOnChange="false">
        <sqlFile path="V85__subscription_plan_feature_keys_jsonb.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V86 â€” Per-region policy table (MVP-c batch / MVP-9c)
         Adds platform.region_policy keyed on OrganizationRegion code
         with optional retention / export-format / target deployment
         overrides. Seed row per current region; EU defaults to
         GDPR_PORTABILITY export. Strictly additive.
         ================================================================= -->
    <changeSet id="V86-region-policy" author="hms-team" runOnChange="false">
        <sqlFile path="V86__region_policy.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V87 â€” Server-side persisted audit saved searches (MVP-c batch / MVP-8c)
         Promotes MVP-8b's per-operator localStorage saved searches to a
         server-side table with cross-device sync + cross-operator
         sharing via a `shared` flag. Strictly additive.
         ================================================================= -->
    <changeSet id="V87-audit-saved-search" author="hms-team" runOnChange="false">
        <sqlFile path="V87__audit_saved_search.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V88 â€” Integration health time-series (MVP-c batch / MVP-3b)
         Adds clinical.integration_health_event so the Integration Health
         Console can render per-integration 24h sparklines. The existing
         IntegrationHealthSnapshot powers the inventory view; the event
         table powers the history drawer. Strictly additive.
         ================================================================= -->
    <changeSet id="V88-integration-health-event" author="hms-team" runOnChange="false">
        <sqlFile path="V88__integration_health_event.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V89 â€” Bridges-style per-message log + DLQ (MVP-c3 / Tier 2 #5)
         Adds clinical.integration_message_event for the operator's
         message-trace + dead-letter-queue + replay surface. Sibling of
         V88: V88 records probe outcomes ("is this integration healthy
         right now?"); V89 records the actual partner-protocol payloads
         that crossed the wire (HL7, FHIR, X12, proprietary REST).
         The recorder is best-effort and tolerates the table being
         absent, so the migration is strictly additive and rollback-safe.
         ================================================================= -->
    <changeSet id="V89-integration-message-event" author="hms-team" runOnChange="false">
        <sqlFile path="V89__integration_message_event.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V90 â€” Hospital-name search index (super-admin scope-chip typeahead)
         Adds a functional B-tree index on LOWER(hospital.hospitals.name)
         to back the GET /api/super-admin/hospitals/search endpoint
         introduced in docs/super-admin-cross-tenant-design.md.
         Originally added decoratively for substring queries; F2
         (2026-05-06) flipped HospitalRepository.searchHospitals to
         prefix match (LIKE 'memo%'), making this index load-bearing
         (true B-tree range scan instead of a sequential scan).
         Strictly additive; CREATE INDEX IF NOT EXISTS.
         ================================================================= -->
    <changeSet id="V90-hospital-name-search-index" author="hms-team" runOnChange="false">
        <sqlFile path="V90__hospital_name_search_index.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V91 â€” (created_at DESC) indexes on the seven clinical parent tables
         Closes F4 from docs/super-admin-cross-tenant-design.md. Adds
         dedicated (created_at DESC) B-tree indexes on:
           - clinical.consultations
           - clinical.encounters
           - clinical.prescriptions
           - clinical.treatment_plans
           - lab.lab_orders
           - admissions
           - general_referrals
         Lays the groundwork for the cursor/keyset pagination follow-up
         from design call #2 and gives the cross-tenant `recent-*`
         endpoints a covering index on their secondary sort key
         (each entity's clinical-time field is the primary sort, with
         created_at DESC as the tiebreaker / null-safety fallback â€”
         see SuperAdminDashboardServiceImpl.getRecent*).
         Strictly additive; all CREATE INDEX IF NOT EXISTS.
         ================================================================= -->
    <changeSet id="V91-clinical-created-at-desc-indexes" author="hms-team" runOnChange="false">
        <sqlFile path="V91__clinical_created_at_desc_indexes.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V92 â€” 1:1 enforcement between Encounter and DischargeSummary
         Defensively dedupes any pre-existing duplicate discharge_summaries
         rows (keeping the original by created_at), then adds a UNIQUE
         constraint on (encounter_id) so concurrent /me/patient/
         after-visit-summaries backfills can no longer race-create
         duplicates. The dedupe + constraint addition share a single DO $$
         block, so splitStatements MUST be false (same reasoning as V9 /
         V61 / V63).
         Originally landed via PR #259 (fix(avs): address Copilot review
         findings) but the changelog entry was missed; this entry registers
         the existing migration with Liquibase. The SQL is idempotent â€”
         the DO block guards on pg_constraint â€” so re-applying on
         deployments that already ran V92 directly is a no-op.
         ================================================================= -->
    <changeSet id="V92-discharge-summaries-unique-encounter-id" author="hms-team" runOnChange="false">
        <sqlFile path="V92__discharge_summaries_unique_encounter_id.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V93 â€” v1.0 / Clinical Safety / CDS Hooks expansion (roadmap row 2)
         Strengthens the RxNorm binding on
         clinical.medication_catalog_items so the new order-select and
         medication-prescribe hook services can resolve a catalog row
         from a FHIR CodeableConcept whose only typed coding is the
         canonical RxNorm system.
         Adds (additive, idempotent):
           1. Pre-flight UPDATE NULL-ing any malformed rxnorm_code so
              the CHECK constraint can be added safely.
           2. CHECK constraint chk_med_catalog_rxnorm_format pinning
              rxnorm_code to ^[0-9]{1,12}$ â€” same regex
              TerminologyCodes.RXNORM enforces in Java. Wrapped in a
              DO $$ + pg_constraint guard so re-running is a no-op.
           3. Partial index idx_med_catalog_rxnorm_active backing the
              new MedicationCatalogItemRepository
              .findActiveByHospitalIdAndRxnormCode lookup used by both
              new hooks and by the extended hms-medication-allergy-check
              (drug-drug interaction add-on, roadmap row 3).
           4. Optional tall_man_name VARCHAR(200) column for ISMP
              confusable-name pairs surfaced in card detail by
              MedicationPrescribeRulesCdsService.
         splitStatements MUST be false because the CHECK-constraint
         step uses a DO $$ ... $$; block (same reasoning as V9 / V61 /
         V63 / V92).
         ================================================================= -->
    <changeSet id="V93-cds-rxnorm-bindings" author="hms-team" runOnChange="false">
        <sqlFile path="V93__cds_rxnorm_bindings.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V94: v1.0 / Pharmacy / T-68 offline dispense queue (roadmap row 4)
         Adds an optional idempotency_key column on clinical.dispenses + a
         partial UNIQUE index so the offline IndexedDB queue can replay
         POST /pharmacy/dispense without producing duplicate rows / stock
         decrements / ready-for-pickup SMS. Pure DDL â€” splitStatements true;
         every statement is wrapped IF NOT EXISTS so re-running is a no-op.
         ================================================================= -->
    <changeSet id="V94-dispense-idempotency-key" author="hms-team" runOnChange="false">
        <sqlFile path="V94__dispense_idempotency_key.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V95: Medication catalog becomes a platform / LNME catalog
         The original V43 schema modelled medication_catalog_items as
         strictly hospital-scoped (hospital_id NOT NULL, UNIQUE
         (hospital_id, code)), forcing every hospital to redundantly
         insert "Amoxicillin", "ParacÃ©tamol", etc.  This migration
         relaxes hospital_id to optional so a single super-admin can
         curate the platform catalog (NULL = global / LNME entry visible
         to every tenant), while leaving the door open for hospital-
         specific entries.  The single composite UNIQUE constraint is
         split into two partial unique indexes so the two scopes can't
         shadow each other (Postgres treats NULLs as distinct in a
         multi-column UNIQUE, so without the split multiple globals
         with the same code would silently coexist).
         Pure DDL; splitStatements true; idempotent (DROP CONSTRAINT IF
         EXISTS + CREATE UNIQUE INDEX IF NOT EXISTS).
         ================================================================= -->
    <changeSet id="V95-medication-catalog-platform-scope" author="hms-team" runOnChange="false">
        <sqlFile path="V95__medication_catalog_platform_scope.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V96: De-duplicate user_role_hospital_assignment and prevent it
         from happening again at the schema level. The seeder's
         createAssignment used to insert unconditionally on every boot,
         leaving duplicate (user, hospital, role) rows. Cleans up
         existing duplicates (and their dependent permissions, which
         have no DB-level FK on assignment_id) and adds partial unique
         indexes covering both hospital-scoped and global assignments.
         Pure DDL + DELETE on dev-discovered drift; splitStatements
         true so the SQL file is executed as separate statements
         (Liquibase still runs the whole changeSet in one transaction
         unless runInTransaction="false").
         ================================================================= -->
    <changeSet id="V96-deduplicate-user-role-hospital-assignment" author="hms-team" runOnChange="false">
        <sqlFile path="V96__deduplicate_user_role_hospital_assignment.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

     <!-- =================================================================
         V97 â€” Hospital tenant isolation mode (roadmap row 33, v2.0)
         Adds isolation_mode + tenant_schema_name to hospital.hospitals
         so hospitals with strong isolation needs (military, foreign-
         private) can opt in to schema-per-tenant. Pure metadata; no
         data movement and no behavior change until
         app.tenancy.schema-isolation.enabled flips on. The DO-blocks
         around the CHECK constraints make the migration idempotent
         (Liquibase keeps a record but the SQL itself can be re-run
         against a recovered DB without erroring). splitStatements
         false because the DO $$ ... $$ blocks contain semicolons.
         ================================================================= -->
    <changeSet id="V97-hospital-tenant-isolation-mode" author="hms-team" runOnChange="false">
        <sqlFile path="V97__hospital_tenant_isolation_mode.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                     stripComments="true"/>
     </changeSet>

     <!-- =================================================================
         V98 â€” Lab result idempotency for inbound ORU^R01 retransmissions
         (roadmap row 23, v1.1 / Interop HL7).
         Adds source_message_control_id (MSH-10) + partial unique index
         so analyzer retransmits (Mindray / Sysmex / cobas) collapse to
         a single lab_result row instead of duplicating on every
         retry. V97 is reserved for the schema-per-tenant feature
         branch â€” V98 is the next free slot from develop's perspective.
         splitStatements true; no DO-blocks here, just ALTER + CREATE
         INDEX IF NOT EXISTS.
         ================================================================= -->
    <changeSet id="V98-lab-result-source-message-control-id" author="hms-team" runOnChange="false">
        <sqlFile path="V98__lab_result_source_message_control_id.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

     <!-- =================================================================
         V99 â€” ADT external-visit reconciliation columns
         (roadmap row 24, v1.1 / Interop HL7).
         Adds external_visit_number + external_sending_application +
         external_sending_facility + external_message_control_id (all
         VARCHAR 255, nullable) on admissions and clinical.encounters,
         each with a partial composite unique index scoped per
         (sender app, sender facility, visit number, hospital_id) for
         rows where the visit number is non-null. Backs the ADT
         visit-sync projection (MllpInboundAdtVisitProjectionService).
         JPA mappings on Admission + Encounter reference all four
         columns from this PR onward, so Liquibase MUST apply the
         migration on startup or entity load will fail with "column
         does not exist". Pure DDL: ADD COLUMN IF NOT EXISTS x 8 +
         CREATE UNIQUE INDEX IF NOT EXISTS x 2; splitStatements true.
         ================================================================= -->
    <changeSet id="V99-adt-external-visit-reconciliation" author="hms-team" runOnChange="false">
        <sqlFile path="V99__adt_external_visit_reconciliation.sql"
                     relativeToChangelogFile="true"
                     splitStatements="true"
                     stripComments="true"/>
     </changeSet>

     <!-- =================================================================
         V100 â€” LOINC binding for hms-patient-view CDS Hook
         (roadmap row 26, v1.1 / Clinical Safety).
         Adds clinical.patient_problems.loinc_code (VARCHAR 20) and
         loinc_display (VARCHAR 255), both nullable. The PatientProblem
         JPA mapping references both columns from this PR onward, so
         Liquibase MUST apply this migration on startup â€” otherwise the
         entity load fails with "column does not exist". V99 is reserved
         for the ADT^A01/A04/A08 visit-sync feature branch
         (feat/v1.1-adt-admission-encounter-sync); V100 is the next free
         slot from this branch's perspective.
         Pure DDL: ADD COLUMN IF NOT EXISTS x 2; splitStatements true.
         ================================================================= -->
    <changeSet id="V100-patient-problem-loinc-binding" author="hms-team" runOnChange="false">
        <sqlFile path="V100__patient_problem_loinc_binding.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V101 â€” FHIR write idempotency index on patient registrations
         (roadmap row 20, v1.1 / Backend / "FHIR write API").
         Adds a partial unique index on
         clinical.patient_hospital_registrations (hospital_id, LOWER(mrn))
         WHERE is_active = true so FHIR conditional-create
         (If-None-Exist) and HL7 ADT idempotency can rely on at most
         one active MRN per hospital. Excludes inactive rows and
         legacy NULL/blank MRNs. V100 is the prior slot
         (LOINC binding); V101 is the next free.
         Pure DDL: CREATE UNIQUE INDEX IF NOT EXISTS x 1.
         ================================================================= -->
    <changeSet id="V101-fhir-write-patient-idempotency" author="hms-team" runOnChange="false">
        <sqlFile path="V101__fhir_write_patient_idempotency.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V102 â€” Hospital scope for announcements.
         Adds nullable public.announcement.hospital_id plus a covering
         (hospital_id, date DESC) index and FK to hospital.hospitals so
         Nurse Station announcements and counts can be tenant-scoped.
         The FK guard uses a DO block, so splitStatements is false.
         ================================================================= -->
    <changeSet id="V102-announcement-hospital-scope" author="hms-team" runOnChange="false">
        <sqlFile path="V102__announcement_hospital_scope.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V103 â€” Per-hospital ADT intake-provider configuration.
         Roadmap row 24 follow-on. Adds platform.adt_intake_provider_configs
         (one row per hospital) so MllpInboundAdtVisitProjectionService can
         auto-create an Admission when an ADT^A01 arrives with an
         unmatched visit-number triplet. Strictly additive:
         CREATE TABLE IF NOT EXISTS + two CREATE INDEX IF NOT EXISTS +
         COMMENT ON. Hard FK on hospital_id only; provider/department
         UUIDs are application-layer-validated to tolerate staff/dept
         re-seeds. V102 is the prior slot (announcement hospital scope);
         V104 is the next free.
         ================================================================= -->
    <changeSet id="V103-adt-intake-provider-config" author="hms-team" runOnChange="false">
        <sqlFile path="V103__adt_intake_provider_config.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V104 â€” ADT auto-create default_assignment_id column.
         Roadmap row 24 follow-on (A04 Encounter-only auto-create).
         Adds nullable platform.adt_intake_provider_configs.default_assignment_id
         so the projection service can stamp Encounter.assignment when
         provisioning from an A04. Strictly additive (ADD COLUMN IF NOT
         EXISTS); the new column stays NULL on V103-era rows that only
         enabled A01 auto-create. V103 is the prior slot; V105 is next free.
         ================================================================= -->
    <changeSet id="V104-adt-intake-default-assignment" author="hms-team" runOnChange="false">
        <sqlFile path="V104__adt_intake_default_assignment.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V105 â€” KPI dashboard materialized views.
         Roadmap row 32 follow-on. Adds three matviews
         (clinical.kpi_door_to_doctor_daily,
          clinical.kpi_dispense_lead_time_daily,
          clinical.kpi_no_show_rate_daily) pre-aggregated per
         (hospital_id, day) so KpiDashboardServiceImpl can stop
         re-scanning source tables on every dashboard load. Each view
         carries a unique (hospital_id, day_bucket) index so
         REFRESH MATERIALIZED VIEW CONCURRENTLY works for the
         scheduled refresher. PostgreSQL-only â€” the
         <dbms type="postgresql"/> preCondition with
         onFail="MARK_RAN" skips the changeset entirely on H2
         (test) so the existing on-the-fly fallback path keeps
         tests bit-for-bit unchanged. The service prefers the matview
         when app.analytics.kpi.materialized-views.enabled=true AND
         the view exists; otherwise it falls back to the existing
         native SQL path.
         ================================================================= -->
    <changeSet id="V105-kpi-dashboard-materialized-views" author="hms-team" runOnChange="false">
        <preConditions onFail="MARK_RAN">
            <dbms type="postgresql"/>
        </preConditions>
        <sqlFile path="V105__kpi_dashboard_materialized_views.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="true"/>
    </changeSet>

    <!-- =================================================================
         V106 â€” Cross-service link PostpartumCarePlan â†’ NewbornAssessment.
         Roadmap row 41 follow-on item (1) "cross-service workflow
         integration". Adds nullable newborn_assessment_id column to
         clinical.postpartum_care_plans, FK to clinical.newborn_assessments
         with NO ACTION on delete (defensive â€” operator must null the link
         before deleting an assessment). Strictly additive: ADD COLUMN IF
         NOT EXISTS + DO-block FK guard + CREATE INDEX IF NOT EXISTS.
         splitStatements must be false because the FK guard is a PL/pgSQL
         DO block.
         ================================================================= -->
    <changeSet id="V106-postpartum-newborn-assessment-link" author="hms-team" runOnChange="false">
        <sqlFile path="V106__postpartum_newborn_assessment_link.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V107 â€” Phone-first registration + IKODDI SMS OTP. New
         security.phone_otp_challenges table (one row per OTP send holding
         the opaque IKODDI verification key), clinical.patients
         .phone_verified_at, and email DROP NOT NULL on clinical.patients
         + security.users: most patients have a phone number but no email
         address. Additive plus two constraint relaxations; the unique
         email indexes stay valid (Postgres unique indexes ignore NULLs).
         ================================================================= -->
    <changeSet id="V107-phone-otp-and-optional-email" author="hms-team" runOnChange="false">
        <sqlFile path="V107__phone_otp_and_optional_email.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V108 â€” Nurse handoffs (P0 #1). New clinical.nurse_handoffs table
         backing real SBAR shift/transfer handoff records; replaces the
         synthetic rows the nurse-station handoffs tab used to render.
         Purely additive.
         ================================================================= -->
    <changeSet id="V108-nurse-handoffs" author="hms-team" runOnChange="false">
        <sqlFile path="V108__nurse_handoffs.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V109 â€” Critical-value escalation (P0 #5). Notification/escalation
         stamps on lab.lab_results + partial index for the sweep. Purely
         additive.
         ================================================================= -->
    <changeSet id="V109-lab-critical-escalation" author="hms-team" runOnChange="false">
        <sqlFile path="V109__lab_critical_escalation.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V110 â€” Repair clinical.nurse_handoffs schema drift on dev: the
         original V108 deploy died mid-changeset leaving a table that
         does not match the entity (validate failed on missing columns).
         ADD COLUMN IF NOT EXISTS for every mapped column; no-op on
         databases where V108 created the full table.
         ================================================================= -->
    <changeSet id="V110-nurse-handoffs-schema-repair" author="hms-team" runOnChange="false">
        <sqlFile path="V110__nurse_handoffs_schema_repair.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V111 â€” Labor & Delivery (P1 #6, roadmap row 41 "antepartum /
         partogram"). clinical.labor_episodes + labor_partograph_entries
         (+alerts) + delivery_records (+alerts), and the nullable
         newborn_assessments.delivery_record_id back-link the OB scope
         audit deferred. Purely additive; IF NOT EXISTS throughout.
         ================================================================= -->
    <!-- splitStatements=false: the file contains a DO $$â€¦$$ block whose
         internal semicolons the default splitter would break (V106 precedent);
         the whole file executes as one atomic statement batch. -->
    <changeSet id="V111-labor-partograph" author="hms-team" runOnChange="false">
        <sqlFile path="V111__labor_partograph.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V112 â€” SMS appointment reminders (P1 #7): reminder_sent_at
         idempotency stamp + partial sweep index on clinical.appointments.
         Purely additive; IF NOT EXISTS throughout.
         ================================================================= -->
    <changeSet id="V112-appointment-reminders" author="hms-team" runOnChange="false">
        <sqlFile path="V112__appointment_reminders.sql"
                 relativeToChangelogFile="true"
                 splitStatements="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V113 â€” clinical.patients.user_id integrity.
         Adds the unique index the @OneToOne(unique = true) mapping always
         claimed but the schema never had (same defect V8 fixed for
         hospital.staff). Conditional: never fails the deploy, and never
         deletes patient rows â€” see the file header for why.
         splitStatements=false: the file is a single DO $$ ... $$ block.
         ================================================================= -->
    <changeSet id="V113-patients-user-id-integrity" author="hms-team" runOnChange="false">
        <sqlFile path="V113__patients_user_id_integrity.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V114 â€” clinical.prescriptions.refills_used.
         Closes the refill â†’ dispense loop: approving a refill request now
         returns the prescription to a dispensable state, and the fill
         accounting needs to know how many times that has happened so a
         partial refill fill is not mistaken for a completed one.
         ================================================================= -->
    <changeSet id="V114-prescription-refills-used" author="hms-team" runOnChange="false">
        <sqlFile path="V114__prescription_refills_used.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V115 â€” clinical.patient_hospital_registrations.patient_id FK.
         The schema has no foreign keys at all (V1 came from Hibernate
         SchemaExport, which emits none), so a registration could point at
         a deleted patient. One such row returned 500 for the entire
         registrations desk. ON DELETE CASCADE mirrors the orphanRemoval
         already on Patient.hospitalRegistrations, so it changes no ORM
         behaviour and can never block a delete; NOT VALID lets the
         surviving orphan stay for reconciliation instead of failing the
         deploy. splitStatements=false: the file is a single DO $$ block.
         ================================================================= -->
    <changeSet id="V115-registration-patient-fk" author="hms-team" runOnChange="false">
        <sqlFile path="V115__registration_patient_fk.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V116 â€” lab_results critical-value read-back + escalation levels.
         Shipped in PR #451 but never registered here, so the columns were
         never created while the entity mapping already declared them.
         ddl-auto=validate then refused to build the SessionFactory and the
         whole application failed to start: "missing column
         [critical_escalation_level] in table [lab.lab_results]". A
         migration file on disk is inert until it appears in this file â€”
         that is the only thing that makes it run.
         ================================================================= -->
    <changeSet id="V116-critical-value-readback" author="hms-team" runOnChange="false">
        <sqlFile path="V116__critical_value_readback_and_escalation_levels.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V117 â€” general_referrals.appointment_id. Same omission as V116
         (PR #453): the column backs a real mapping that validate checks.
         ================================================================= -->
    <changeSet id="V117-referral-appointment-link" author="hms-team" runOnChange="false">
        <sqlFile path="V117__referral_appointment_link.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V118 â€” prescription signature columns. Same omission as V116
         (PR #455). splitStatements=false: the file ends in a DO $$ block
         that adds fk_prescription_signed_by.
         ================================================================= -->
    <changeSet id="V118-prescription-signature" author="hms-team" runOnChange="false">
        <sqlFile path="V118__prescription_signature.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V119 â€” delivery tracking for lab.instrument_outbox.
         OML/ORU messages were queued since the lab module shipped and
         never transmitted, because no sender existed. A sender that
         retries needs somewhere to record attempts, the last failure
         text and the instant the backoff is measured from. Strictly
         additive with defaults, so existing PENDING rows start at zero.
         ================================================================= -->
    <changeSet id="V119-instrument-outbox-delivery-tracking" author="hms-team" runOnChange="false">
        <sqlFile path="V119__instrument_outbox_delivery_tracking.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V120 â€” drug-interaction knowledge-base expansion.
         The checking pipeline is real at prescribe, dispense and CDS-Hooks,
         but the KB behind it was V63's 12-pair seed â€” and a checker with
         almost nothing to check against is worse than none, because green
         reads as "no interaction" when it means "not in our twelve rows".
         Adds the pairs a WHO-Essential-Medicines formulary most needs, each
         citing its reference. splitStatements=false: DO $$ block.

         Re-added after the develop merge dropped it: this branch and
         develop both appended just before the closing tag, and resolving
         that overlap kept develop's V116-V119 and lost this block. The
         file stayed on disk, so the loss was invisible until
         MigrationRegistrationTest and LiquibaseSchemaIT's 25-row seed
         assertion both failed.
         ================================================================= -->
    <changeSet id="V120-drug-interaction-kb-expansion" author="hms-team" runOnChange="false">
        <sqlFile path="V120__drug_interaction_kb_expansion.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V121 â€” slot inventory foundation (visit types, session templates,
         generated slots). Scheduling could book an Appointment against an
         arbitrary (staff, date, time) but nothing described what a clinic
         session IS, so nothing could answer "when is this clinician next
         free for a 30-minute follow-up?" â€” the question that blocks patient
         self-scheduling, waitlist auto-offer and utilisation reporting
         alike. New tables, so they carry REAL foreign keys.

         Re-added for the THIRD time. Every migration's changeSet goes
         immediately before </databaseChangeLog>, so any two branches that
         add one collide on the same lines; resolving that collision by
         taking develop drops the branch's own migration while leaving its
         .sql on disk, which looks like nothing happened. GitHub's "Update
         branch" button resolves it exactly that way. MigrationRegistrationTest
         is what turns this from a silent production outage into a red build.
         ================================================================= -->
    <changeSet id="V121-slot-inventory-foundation" author="hms-team" runOnChange="false">
        <sqlFile path="V121__slot_inventory_foundation.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V122 â€” height_cm + head_circumference_cm on patient_vital_signs
         (P3 item 18, growth charts). weight_kg has existed since V1 with
         no other anthropometric; triage-entered height was accepted and
         silently discarded. Additive, nullable, idempotent. No DO block,
         so no splitStatements attribute.
         ================================================================= -->
    <changeSet id="V122-vitals-anthropometrics" author="hms-team" runOnChange="false">
        <sqlFile path="V122__vitals_anthropometrics.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V123 â€” clinical.intake_output_entries (P3 item 18, flowsheets/I&O).
         First numeric I&O surface; the old "INTAKE_OUTPUT" was a free-text
         task-category string with no volumes. New table with REAL FKs,
         observation-time/late-entry quad. No DO block, so no
         splitStatements attribute.
         ================================================================= -->
    <changeSet id="V123-intake-output-entries" author="hms-team" runOnChange="false">
        <sqlFile path="V123__intake_output_entries.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V124 â€” lab.micro_culture_results / micro_isolates /
         micro_susceptibilities (P3 item 19, microbiology). First
         micro-shaped tables: culture report on a lab order, organisms per
         culture, susceptibility rows per organism. New tables with REAL
         FKs. No DO block, so no splitStatements attribute.
         ================================================================= -->
    <changeSet id="V124-microbiology" author="hms-team" runOnChange="false">
        <sqlFile path="V124__microbiology.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V125 â€” encounter-note sign/co-sign ceremony columns (P3 item 20).
         Kills the client-asserted note signature (the pre-V118
         anti-pattern): server-resolved signer + SHA-256 digest, plus the
         requires_cosign/cosigned_by/cosigned_at attestation pair.
         Contains a DO $$ block (guarded FK adds), so splitStatements
         must be false.
         ================================================================= -->
    <changeSet id="V125-encounter-note-signature" author="hms-team" runOnChange="false">
        <sqlFile path="V125__encounter_note_signature.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"
                 splitStatements="false"/>
    </changeSet>

    <!-- =================================================================
         V126 â€” registration extras (P3 item 21): patient photo columns
         (path + type; binary streamed through an authenticated endpoint,
         never the permitAll /uploads/** handler), consent-to-treat
         records (the pre-check-in consent checkbox was write-only), and
         guarantor accounts. New tables with REAL FKs. No DO block, so no
         splitStatements attribute.
         ================================================================= -->
    <changeSet id="V126-registration-extras" author="hms-team" runOnChange="false">
        <sqlFile path="V126__registration_extras.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V127 â€” platform.platform_downtime_state (P3 item 23a, read-only
         continuity mode). Singleton row (V80 security_revocations
         pattern) enforced by an HTTP filter â€” deliberately not DB-level
         read-only, which would break login and silently drop audit rows.
         No DO block, so no splitStatements attribute.
         ================================================================= -->
    <changeSet id="V127-downtime-state" author="hms-team" runOnChange="false">
        <sqlFile path="V127__downtime_state.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V128 â€” recall lists + waitlist auto-offer + slot booking (P3
         item 22, per the 2026-08-22 decision: the Appointment owns the
         time). Slot @Version column, waitlist offer columns (+guarded
         FK), scheduling.patient_recalls. Contains a DO $$ block, so
         splitStatements must be false.
         ================================================================= -->
    <changeSet id="V128-recall-waitlist-booking" author="hms-team" runOnChange="false">
        <sqlFile path="V128__recall_waitlist_booking.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"
                 splitStatements="false"/>
    </changeSet>

    <!-- =================================================================
         V129 â€” persistent FHIR bulk-export jobs (P3 item 24, roadmap
         row 21 follow-on): platform.fhir_bulk_export_jobs +
         fhir_bulk_export_files replace the in-memory job map so the
         @Scheduled NDJSON runner can finish what the kickoff started.
         No DO block, so no splitStatements attribute.
         ================================================================= -->
    <changeSet id="V129-fhir-bulk-export-jobs" author="hms-team" runOnChange="false">
        <sqlFile path="V129__fhir_bulk_export_jobs.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V130 â€” scheduled reports + NEWS2 early-warning capture (P3 item
         25): on_oxygen + consciousness_level on patient_vital_signs +
         the NEWS2 BPA protocol seed; platform.report_definitions +
         report_runs with the UNIQUE (definition, period) exactly-once
         claim. No DO block, so no splitStatements attribute.
         ================================================================= -->
    <changeSet id="V130-reports-and-news2" author="hms-team" runOnChange="false">
        <sqlFile path="V130__reports_and_news2.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V131 - multi-OBX ORU^R01 ingestion: source_observation_set_id +
         test_code + reference_range on lab.lab_results, and the V98
         dedup index redefined to include the per-OBX discriminator so
         sibling rows of one message no longer collide. No DO block, so
         no splitStatements attribute.
         ================================================================= -->
    <changeSet id="V131-lab-result-multi-obx" author="hms-team" runOnChange="false">
        <sqlFile path="V131__lab_result_multi_obx.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V132 - imaging report signing ceremony: signature_algorithm +
         signature_value on clinical.imaging_reports, plus the unsigned
         worklist and unacknowledged-critical partial indexes the
         reading room reads. No DO block, so no splitStatements
         attribute.
         ================================================================= -->
    <changeSet id="V132-imaging-report-signature" author="hms-team" runOnChange="false">
        <sqlFile path="V132__imaging_report_signature.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V133 - critical-imaging-finding notification ledger:
         critical_notified_at + critical_escalated_at +
         critical_escalation_level on clinical.imaging_reports, plus the
         partial index the escalation sweep reads. The lab equivalents
         live on lab.lab_results and cannot be shared. No DO block, so
         no splitStatements attribute.
         ================================================================= -->
    <changeSet id="V133-imaging-critical-escalation" author="hms-team" runOnChange="false">
        <sqlFile path="V133__imaging_critical_escalation.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V134 - blood bank and transfusion: patient_blood_groups (the
         lab-verified type and screen, distinct from the free-text
         clinical.patients.blood_type), transfusion_requests, blood_units,
         transfusion_crossmatches, transfusion_administrations and
         transfusion_reactions. No DO block, so no splitStatements
         attribute.
         ================================================================= -->
    <changeSet id="V134-blood-bank-transfusion" author="hms-team" runOnChange="false">
        <sqlFile path="V134__blood_bank_transfusion.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V135 - death and mortality: clinical.patients.deceased_at (the
         flag every sweep and worklist reads) plus clinical.death_records
         (the certificate, one row per patient). V133 belongs to the
         imaging-escalation branch still in flight; the number is reserved
         so nothing collides on merge. No DO block, so no splitStatements
         attribute.
         ================================================================= -->
    <changeSet id="V135-death-and-mortality" author="hms-team" runOnChange="false">
        <sqlFile path="V135__death_and_mortality.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V136 - isolation precautions: clinical.isolation_precautions, a
         child table rather than an enum on the admission because concurrent
         precautions are normal (a VHF is contact AND droplet). Active is
         ended_at IS NULL; rows are discontinued, never deleted, because
         contact tracing asks what WAS in force. V137 is reserved for the
         transfer-order branch so the numbers cannot collide on merge. No DO
         block, so no splitStatements attribute.
         ================================================================= -->
    <changeSet id="V136-isolation-precautions" author="hms-team" runOnChange="false">
        <sqlFile path="V136__isolation_precautions.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V137 - in-app transfer orders: clinical.transfer_orders. An
         orchestration and audit layer over the Admission.bed <-> Bed.status
         invariant BedAssignmentService has owned since P0 #4, not a second
         writer of it. Two steps, because a transfer is ordered by one person
         and carried out by another; the destination is held RESERVED in
         between so it cannot be allocated twice. No DO block, so no
         splitStatements attribute.
         ================================================================= -->
    <changeSet id="V137-transfer-orders" author="hms-team" runOnChange="false">
        <sqlFile path="V137__transfer_orders.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V138 - dispense-time verification. Gives the stock lot a
         server-minted barcode (the product scan target did not exist; the
         wristband from #475 is only the patient half) and the dispense a
         verification record mirroring the eMAR's five-rights columns. The
         scan stays optional for paper-fallback sites, but the two server
         checks it enables are always enforced: expiry_date had NO reader
         anywhere in the application, and the lot's drug was never compared
         to the prescription's. No DO block, so no splitStatements
         attribute.
         ================================================================= -->
    <changeSet id="V138-dispense-verification" author="hms-team" runOnChange="false">
        <sqlFile path="V138__dispense_verification.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V139 - pharmacist verification before administration. SCOPED to
         controlled substances and requiresCosign prescriptions, not
         universal: clinical.pharmacies has no operating-hours model, so a
         universal gate could never fail open when the single pharmacist is
         off-shift. Verification is CLEARED on every prescription update,
         because updatePrescription has no status guard and a SIGNED
         prescription's drug and dose remain mutable - a stale stamp would
         assert a check that never happened. No DO block, so no
         splitStatements attribute.
         ================================================================= -->
    <changeSet id="V139-pharmacist-verification" author="hms-team" runOnChange="false">
        <sqlFile path="V139__pharmacist_verification.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V140 - provider credentialing renewal. license_number and
         license_expiry_date have existed since V1 and nothing ever renewed,
         verified or alerted on them. Adds a renewal HISTORY table rather
         than columns alone - the opposite call from V139 and deliberately
         so: a licence is renewed repeatedly, and "was this clinician
         licensed on the day they prescribed that" is only answerable from
         history. license_alert_stage keeps the daily sweep from
         re-notifying the same expiry until administrators stop reading the
         category. Blocks nothing: whether an expired licence should stop
         clinical work is a policy decision, not a schema one. No DO block,
         so no splitStatements attribute.
         ================================================================= -->
    <changeSet id="V140-provider-credentialing" author="hms-team" runOnChange="false">
        <sqlFile path="V140__provider_credentialing.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V141 - patient key on the audit ledger. The patient portal
         already shipped a "who viewed my records" tab; it queried
         (target_entity_type='PATIENT', target_resource_id=patientId),
         which is a convention only three of the six patient-related
         emitters follow. Break-the-glass keys on the SESSION id and
         eligibility checks on the CHECK id, so emergency access to a
         chart - the one category the page exists for - never appeared
         on it, and neither did disclosures to an insurance scheme.
         Adds the column the query should have had, backfills the three
         shapes whose patient is derivable, and leaves the rest NULL
         rather than implying completeness. No FK: audit rows must
         outlive purged patients. DO block, so splitStatements="false".
         ================================================================= -->
    <changeSet id="V141-audit-patient-key" author="hms-team" runOnChange="false">
        <sqlFile path="V141__audit_patient_key.sql"
                 relativeToChangelogFile="true"
                 splitStatements="false"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V142 - unstrand RESCHEDULED appointments. The reschedule modal
         saved the new date together with status RESCHEDULED, which every
         consumer treats as terminal: no action buttons on the detail
         page, no check-in icon in the reception queue, and
         checkInPatient rejects it outright. The result was an
         appointment with a correct date and time, a patient expected to
         arrive, and no way to check them in. Moves today-and-later rows
         to SCHEDULED, which the transition map always allowed and
         nothing ever performed. Past-dated rows are left alone - those
         are history, not a queue. Plain UPDATE, no DO block.
         ================================================================= -->
    <changeSet id="V142-unstrand-rescheduled-appointments" author="hms-team" runOnChange="false">
        <sqlFile path="V142__unstrand_rescheduled_appointments.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

    <!-- =================================================================
         V144 - point_of_care on lab test definitions. A nurse could
         create, edit AND release any lab result; release is what makes a
         result authoritative on the chart, so a nurse's entry sat on the
         same footing as the laboratory's for a panel the nurse never ran.
         Removing nurses outright is wrong - they legitimately run
         glucometer, dipstick and rapid tests at the bedside, and blocking
         those just means the reading goes unrecorded. The distinction is
         WHICH TEST, not who, and nothing expressed it. Deliberately NOT
         seeded: which tests are point-of-care is a per-hospital clinical
         catalogue decision. Every test starts false, so nurses cannot
         enter results until a hospital marks its own - fail closed, and
         the portal says why.
         ================================================================= -->
    <changeSet id="V144-lab-test-definition-point-of-care" author="hms-team" runOnChange="false">
        <sqlFile path="V144__lab_test_definition_point_of_care.sql"
                 relativeToChangelogFile="true"
                 stripComments="false"/>
    </changeSet>

</databaseChangeLog>
