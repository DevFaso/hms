# testing.md
**Backend Tests:** Unit test business logic (JUnit), integration-test APIs (MockMvc or Postman). Cover validation and auth.  
**Frontend Tests:** Test key user flows and UI states (Jest/Protractor). Check loading/empty/error states.  
**General:** Do not mark tasks as “done” unless corresponding tests exist. Use automated graders to enforce style rules if possible.
